FROM node:22-slim AS api
WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
ENV NODE_ENV=production
ENV DB_PATH=/app/data/formflow.db
ENV DB_BACKUP_DIR=/app/data/backups
RUN mkdir -p /app/data/backups
EXPOSE 4450
CMD ["npm", "run", "api:start"]
